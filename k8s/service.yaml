apiVersion: v1
kind: Service
metadata:
  name: admin-gate
  labels:
    app: admin-gate
spec:
  type: ClusterIP
  selector:
    app: admin-gate
  ports:
    - name: http
      port: 80
      targetPort: 80
